# 编号教程

按顺序运行，先理解口径，再接真实数据：

1. `01_认识收益与风险.ipynb`：收益、波动与最大回撤。
2. `02_获取并检查行情.ipynb`：免费 API、标准化和质量校验。
3. `03_第一个无未来函数回测.ipynb`：收盘信号在下一开盘成交。

Notebook 是解释层，核心计算位于 `src/investkb/` 并由 pytest 验证。网络示例默认关闭，避免把接口故障误认为空数据。
